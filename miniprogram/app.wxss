@import "./styles/iconfont.wxss";

page,
view,
text,
swiper,
swiper-item,
navigator,
image {
  padding: 0;
  margin: 0;
  box-sizing: border-box;
}

page{
  /* 定义主题颜色 */
  --themeColor: #990000;
  /* 定义统一字体大小 */
  font-size: 28rpx;
}

image{
  /* 图片大小由外层容器决定 */
   width: 100%;
 }